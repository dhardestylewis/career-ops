# Asset Notes

The default resume attachment for this repo is:

- `resume-dhl-20260630-causal-mle.pdf`

That file is the shared default used by application and outreach scripts through `src/core/resume-asset.mjs`.

Legacy or test-only files in this directory should not be treated as the canonical email attachment unless a task explicitly says otherwise.
